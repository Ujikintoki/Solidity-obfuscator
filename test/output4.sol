// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract TestContract {
    uint public myNumber;

    function setValue(uint _value) public {
        uint tmp_hq1pb4ViyRO = _value * (1 + 1);
        uint localVar = tmp_hq1pb4ViyRO; 
        myNumber = localVar + ((5 + 6) - 1);   
        bool tmp_pWvtEJrWw = (myNumber > (((2704 - 704) / 10) / 2));
        bool flag = tmp_pWvtEJrWw; 
        if (flag) {
            myNumber = (1 - 1);
        }
    }
}